<%= form_for(User.new) do |a| %>
   <li> <%= a.label :username %> 
    <%= a.text_field :username %> </li>
    <li><%= a.label :password %>
    <%= a.text_field :password %></li>
    <%= a.label :admin? %>
    <%= a.check_box :admin %>
    <li><%= a.submit %></li>
<% end %>