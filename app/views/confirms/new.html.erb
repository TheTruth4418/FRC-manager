<%= "Confirm form for #{@event.name}"%>
<%= form_for [@event, Confirm.new] do |a| %>
    <%= a.label :Student %>
    <%= a.collection_select :user_id, @students, :id, :username, include_blank: true %><br>
    <%= render 'form', a: a%>
<%end%>
<%= link_to "Go Back", event_path(@event) %>
