<%= "#{@user.username}'s grades " + @status + "the qualifications for #{@event.name}" %><br>
<%= link_to "Edit Confirm Status", edit_event_confirm_url(@event,@confirm)%><br>
<%= link_to "Go Back",event_url(@event)%>
